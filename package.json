{
  "name": "porschednipro",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n back,front \"npm --prefix back run start:dev\" \"npm --prefix front start\"",
    "dev:back": "npm --prefix back run start:dev",
    "dev:front": "npm --prefix front start"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
